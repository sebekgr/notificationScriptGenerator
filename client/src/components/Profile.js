import React, { Fragment } from 'react';

const Profile = props => (
  <Fragment>
    <h1>Profile</h1>
    <p>Welcome Profile!</p>
    <a href="/auth/logout">Logout </a>
  </Fragment>
);


export default Profile;